name: Pages note

# GitHub Pages is configured to deploy directly from the `main` branch
# (Settings -> Pages -> Source: Deploy from a branch -> main / root).
# No build workflow is needed. This file is kept only as documentation
# and runs nothing automatically.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages deploys from the main branch. Nothing to do."
